# Placeholder — NOT the documentation builder.
#
# ReadTheDocs builds these docs; the MkDocs workflow that lived here was
# retired in fc1d29b ("Let ReadTheDocs be the only documentation builder").
#
# Deleting that file orphaned a run this workflow had already queued
# (32219727413). With no definition at this path, GitHub could neither cancel
# it (HTTP 500, force-cancel included) nor delete it (HTTP 403 — a run must be
# completed before it can be deleted), so it sat queued indefinitely and showed
# up as a permanently "active" run in workflow_attention.sh.
#
# Restoring the name and path gives that orphan something to resolve against so
# it can be cancelled. Manual trigger only: this must never run on push or
# pull_request, or it would resurrect the retired MkDocs build.
#
# Safe to delete once run 32219727413 is gone.

name: Documentation

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Explain
        run: |
          echo "Placeholder workflow. Documentation is built by ReadTheDocs."
          echo "See the comment at the top of .github/workflows/docs.yml."
