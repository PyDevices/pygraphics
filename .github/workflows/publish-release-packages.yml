name: Publish release packages

on:
  release:
    types:
      - published
  workflow_dispatch:
    inputs:
      release-ref:
        description: Exact vX.Y.Z tag to retry
        required: true
        type: string

permissions:
  contents: read

jobs:
  prepare-release:
    runs-on: ubuntu-latest
    outputs:
      release-ref: ${{ steps.release.outputs.release-ref }}
      version: ${{ steps.release.outputs.version }}
    steps:
      - name: Resolve and validate the release tag
        id: release
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name || inputs.release-ref }}
        run: |
          if [[ ! "$RELEASE_TAG" =~ ^v([0-9]+\.[0-9]+\.[0-9]+)$ ]]; then
            echo "Release ref must be an exact vX.Y.Z tag: $RELEASE_TAG" >&2
            exit 1
          fi
          echo "release-ref=$RELEASE_TAG" >> "$GITHUB_OUTPUT"
          echo "version=${BASH_REMATCH[1]}" >> "$GITHUB_OUTPUT"

  build-native-and-wasm-testpypi-wheels:
    needs: prepare-release
    uses: PyDevices/.github/.github/workflows/reusable-build-native-and-wasm-wheels.yml@publishing-v1
    with:
      distribution-name: pydevices-pygraphics
      wheel-filename-prefix: pydevices_pygraphics
      release-ref: ${{ needs.prepare-release.outputs.release-ref }}
      version: ${{ needs.prepare-release.outputs.version }}

  publish-validated-wheels-to-testpypi:
    needs:
      - prepare-release
      - build-native-and-wasm-testpypi-wheels
    runs-on: ubuntu-latest
    environment: testpypi
    permissions:
      id-token: write
    steps:
      - name: Download validated wheels
        uses: actions/download-artifact@v4
        with:
          name: testpypi-pydevices-pygraphics-${{ needs.prepare-release.outputs.version }}
          path: dist

      - name: Publish wheels with TestPyPI Trusted Publishing
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          skip-existing: true

  request-pure-python-mip-publication:
    needs:
      - prepare-release
      - build-native-and-wasm-testpypi-wheels
    uses: PyDevices/.github/.github/workflows/reusable-request-mip-publication.yml@publishing-v1
    with:
      release-ref: ${{ needs.prepare-release.outputs.release-ref }}
      version: ${{ needs.prepare-release.outputs.version }}
      profile: pygraphics
    secrets:
      mip-publication-token: ${{ secrets.MICROPYTHON_LIB_DEPLOY_TOKEN }}
