name: Deploy Pages site

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/deploy.yml"
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The checked-in Pages site has been retired; there is nothing to deploy."
