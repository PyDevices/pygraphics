{
  "name": "pygraphics-cmod-sourcey-docs",
  "private": true,
  "scripts": {
    "generate": "python3 scripts/generate_reference.py",
    "build": "npm run generate && sourcey build --output dist",
    "verify": "node scripts/verify-site.mjs",
    "check": "npm run build && npm run verify"
  },
  "devDependencies": {
    "sourcey": "3.6.5"
  }
}
