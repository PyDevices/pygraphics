# Release notes

## Unreleased

- The native/C-extension build is published to TestPyPI as `pydevices-pygraphics` and imported as `pygraphics`.
- The pure-Python package remains published through micropython-lib / MIP as `pygraphics` for users who do not want to compile their own build.
- Documentation and publishing workflows now reflect this split clearly.
